ASSUME user is root/root

1. create schema dronescrashnburn manually
CREATE SCHEMA `dronescrashnburn`;
--executed ok

2. ensure user root really does have all priv
GRANT ALL PRIVILEGES ON dronescrashnburn.* TO 'root'@'localhost';
--executed ok

3. create the drone table
CREATE TABLE DRONE(
    droneId INT NOT NULL auto_increment, 
    drone_owner_name VARCHAR(50) NOT NULL,
    drone_name VARCHAR(50) NOT NULL,
    drone_type VARCHAR(50) NOT NULL,
    drone_span VARCHAR(50),
    drone_status VARCHAR(50),
    drone_image VARCHAR(50),
    PRIMARY KEY (droneId)
);

4. inspect records in drone table
select * from drone;

5. create a dummy drone set
Basic syntax
Let's look at the basic syntax of the SQL INSERT command shown below. 
INSERT INTO `table_name`(column_1,column_2,...) VALUES (value_1,value_2,...);
HERE 
INSERT INTO `table_name` is the command that tells MySQL server to add new row into a table named `table_name`.
(column_1,column_2,...) specifies the columns to be updated in the  new row
VALUES (value_1,value_2,...) specifies the values to be added into the new row

5A. droneId is auto gen'ed
INSERT INTO `drone`(drone_owner_name, drone_name, drone_type, drone_span, drone_status, drone_image)
 VALUES ("Eagle Ed", "Super Sportster", "Sport", "60 in", "crashed, not fixable - one comment for dummy 1", "resources/images/sport_example.jpg");

5B. droneId is auto gen'ed
INSERT INTO `drone`(drone_owner_name, drone_name, drone_type, drone_span, drone_status, drone_image)
 VALUES ("DB Crash Siegel", "Clound Bound", "Glider", "4 meter", "repaired - no comment for dummy 2", "resources/images/glider_example.jpg");

5C. droneId is auto gen'ed
INSERT INTO `drone`(drone_owner_name, drone_name, drone_type, drone_span, drone_status, drone_image)
 VALUES ("DB Mr. NoBody", "Lawn Mower", "Rotorcraft", "4x12 in", "Flying ballistically - two comments for dummy 3", "resources/images/rotorcraft_example.jpg");

5D. droneId is auto gen'ed
INSERT INTO `drone`(drone_owner_name, drone_name, drone_type, drone_span, drone_status, drone_image)
 VALUES ("DB Talker", "Big Text", "Glider", "too much text", "Hot air - one BIG comment for dummy 4", "resources/images/glider_example.jpg");





============================================================================================
below is original hib example

ASSUME user is root

Step 7: Create Schema in database
CREATE TABLE EMPLOYEE(
    id INT NOT NULL auto_increment, 
    name VARCHAR(50) NOT NULL,
    joining_date DATE NOT NULL,
    salary DOUBLE NOT NULL,
    ssn VARCHAR(30) NOT NULL UNIQUE,
    PRIMARY KEY (id)
);
